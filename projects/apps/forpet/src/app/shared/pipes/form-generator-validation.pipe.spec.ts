import { FormGeneratorValidationPipe } from './form-generator-validation.pipe';

describe('FormGeneratorValidationPipe', () => {
  it('create an instance', () => {
    const pipe = new FormGeneratorValidationPipe();
    expect(pipe).toBeTruthy();
  });
});
