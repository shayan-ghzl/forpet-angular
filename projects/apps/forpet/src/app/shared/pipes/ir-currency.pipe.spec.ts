import { IrCurrencyPipe } from './ir-currency.pipe';

describe('IrCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new IrCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
